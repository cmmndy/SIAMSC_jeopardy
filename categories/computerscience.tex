
% wenn wir auch gifs in das spiel bekommen, könnten wir auch ein bild von einem sortieralgorithmus zeigen
  
  \begin{textarea}[]
  	\only<1>{
  	In this problem, we  ask for the shortest possible route that visits each city exactly once and returns to the origin city.
  	}
  	\only<2>{
  		Who is the travelling salesman problem?
  	}
  \end{textarea}
 
 
 
\begin{textarea}[]
	\only<1>{
		In cryptography, Alice frequently communicates with this person, while Eve tries to listen. 
	}
	\only<2>{
		Who is Bob?
	}
\end{textarea}


\begin{textarea}[]
	\only<1>{
		This abstract machine manipulates symbols on a strip of tape according to a table of rules.
	}
	\only<2>{
		What is a Turing Machine?
	}
\end{textarea}

\begin{textarea}[]
	\only<1>{
		\begin{algorithm}[H]
			\begin{algorithmic}[1]
			\STATE n = length(A)
			\WHILE{ true}
			\STATE swapped = false
			\FOR{$i=1...n-1$}
			\IF {$A[i-1] > A[i]$ }
			\STATE swap( A[i-1], A[i] )
			\STATE swapped = true
			\ENDIF
			\ENDFOR
			\IF{not swapped}
			\RETURN
			\ENDIF
			\ENDWHILE
			\end{algorithmic}
		\end{algorithm}
	}
	\only<2>{
		What is the Bubblesort?
	}
\end{textarea}






\begin{textarea}[]
	\only<1>{
		\begin{algorithm}[H]
			\begin{algorithmic}[1]
				\STATE first = 0
				\STATE second = 1
				\FOR{$c=0...n-1$}
				\IF{$c<=1$}
				\STATE next = c
				\ELSE
				\STATE next = first + second
				\STATE first = second
				\STATE second = next
				\ENDIF
				\ENDFOR
				\RETURN next
			\end{algorithmic}
		\end{algorithm}
	}
	\only<2>{
		What is the Fibonacci Series?
	}
\end{textarea}

